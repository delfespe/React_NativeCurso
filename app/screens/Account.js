import React from "react";
import { Text, View } from "react-native";

export default function Account(params) {
    return(
        <View>
            <Text>Account...</Text>
        </View>
    );
}