import React from "react";
import { Text, View } from "react-native";

export default function Favorites(params) {
    return(
        <View>
            <Text>Favorites...</Text>
        </View>
    );
}